status = "deprecated"
summary = "Cursor merged slash commands into Skills. Existing user- and workspace-level commands can be converted with /migrate-to-skills; author new workflows as Skills."
invocation = ["/command-name"]
docs = "https://cursor.com/docs/context/skills"
notes = [
  "Both user-level and workspace-level commands are convertible to skills (Cursor 2.4+).",
  "Use the Skills surface for new reusable workflows.",
]
